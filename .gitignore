# ── secrets / env (NEVER commit) ───────────────────────────
.env
*.env
!.env.example
config/settings.yaml
config/settings.local.yaml
config/*.local.yaml
*.token
.bws-token
*.key
*.pem
cookies.txt
accounts.db
*credentials*
*secret*
!*.example*

# ── runtime / state ────────────────────────────────────────
*.sqlite
*.sqlite3
*.db
*.log
.codex/
.tmux-dev-session.json
logs/
_scratch/
cerebro/_scratch/
export/

# ── python ─────────────────────────────────────────────────
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# ── os / editor ────────────────────────────────────────────
.DS_Store
.idea/
.vscode/

# skill scratch (goals/plans/research/handover) — never commit
.agents/
explainers/
